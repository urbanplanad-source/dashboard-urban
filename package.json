{
  "name": "urbanplanad-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/ops-check.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test",
    "report:consults:check": "node scripts/check-consults-api.mjs",
    "report:context": "node scripts/fetch-report-context.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
